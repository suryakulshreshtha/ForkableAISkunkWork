# Runtime. Everything else the agent needs is in the standard library.
playwright>=1.40

# Optional but recommended:
pillow>=10.0        # visual regression diffs (without it, visual checks warn)

# Optional, only for `rag.backend = "chroma"`:
# chromadb>=0.5
